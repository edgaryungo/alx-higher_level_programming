#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    for i in range(len(my_list)):
        if mylist[i] % 2 == 0:
            mylist[i] = True
        else:
            mylist[i] = False
    return my_list
